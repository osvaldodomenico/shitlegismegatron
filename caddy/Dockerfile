# Caddy com módulo Cloudflare DNS embutido.
# Imagem pré-buildada pela comunidade caddybuilds — não precisa compilar Go.
# https://github.com/CaddyBuilds/caddy-cloudflare
FROM caddybuilds/caddy-cloudflare:alpine