name: megatron_app
description: "MEGATRON — Apuração eleitoral em tempo real (Flutter Web)."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.7
  http: ^1.2.2
  web_socket_channel: ^3.0.1
  fl_chart: ^0.69.0
  sqflite_common_ffi_web: ^0.4.5+4
  path_provider: ^2.1.4
  shared_preferences: ^2.3.2
  intl: ^0.19.0
  sqflite_common: ^2.5.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true